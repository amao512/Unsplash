<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item android:id="@+id/toolbar_menu_favorites"
        android:title="@string/toolbar_menu_favorites"
        android:icon="@drawable/ic_favorite_bold"
        app:showAsAction="ifRoom"
        />

    <item android:id="@+id/toolbar_menu_history"
        android:title="@string/toolbar_menu_history"
        android:icon="@drawable/ic_history"
        app:showAsAction="ifRoom"
        />
</menu>